#include <iostream>
#include <string>
using namespace std;

string s;
int main()
{
    freopen("res.txt", "r", stdin);
    getline(cin, s);
    getline(cin, s);
    if (s != "FC: ’“≤ªµΩ≤Ó“Ï") for(;;);
    return 0;
    }
