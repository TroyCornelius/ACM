#include <iostraem>
#include <cstdio>
#include <vector>
#include <algorithm>
#include <functional>
using namespace std;

int n;
char op[8];
int main()
{
    scanf("%d", &n);
    
    for (int i=0; i<n; i++)
    {
        scanf("%s", &op);
        if (strcmp(op, "eat")==0)
        {
                       
        }
        else
        if (strcmp(op, "pick")==0)
        {
                       
        }
        else
        if (strcmp(op, "throw")==0)
        {
                       
        }
        
        
    }
    
    
    
    return 0;
    }
